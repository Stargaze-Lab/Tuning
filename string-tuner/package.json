{
  "name": "string-tuner",
  "version": "1.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "deploy": "npm run build && cp -r dist/* ../stargaze/string-tuner/"
  },
  "devDependencies": {
    "vite": "^5.0.0"
  }
}
